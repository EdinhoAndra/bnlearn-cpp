[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bnlearn"
dynamic = ["version"]
authors = [{ name = "Erdogan Taskesen", email = "erdogant@gmail.com" },]
description = "bnlearn is a Python package for Causal Discovery by learning the graphical structure of Bayesian networks, parameter learning, inference and sampling methods."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = ["Python", "causal discovery", "causal learning"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Operating System :: Unix",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "pgmpy @ git+https://github.com/EdinhoAndra/pgmpy-cpp.git@1ae8051ed8c86211e43fe6d32cccd329831c26c9",
    "networkx>=2.7.1",
    "matplotlib>=3.3.4",
    "numpy>=1.24.1",
    "pandas",
    "tqdm",
    "ismember",
    "scikit-learn",
    "python-louvain",
    "packaging",
    "df2onehot",
    "pypickle",
    "tabulate",
    "datazets>=1.1.2",
    "setgraphviz>=1.0.3",
    "lingam",
]

[project.optional-dependencies]
gpu-cu12 = [
    "cupy-cuda12x[ctk]",
]
dev = [
    "pytest",
    "pytest-cov",
]

[project.urls]
Homepage = "https://erdogant.github.io/bnlearn"
Download = "https://github.com/erdogant/bnlearn/archive/{version}.tar.gz"

[tool.setuptools]
packages = ["bnlearn"]
include-package-data = true

[tool.setuptools.dynamic]
version = { attr = "bnlearn.__version__" }
